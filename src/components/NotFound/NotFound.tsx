import React from 'react';
import { NotFoundContainer } from './NotFound.style';

export const NotFound: React.FC = () => {
  return (
    <NotFoundContainer>
      Page not found
    </NotFoundContainer>
  );
}