import styled from 'styled-components';

export const StyledHeader = styled.header`
  padding: 1.5em 1em;

  .link {
    color: #ffffff;
    font-weight: bold;
    text-decoration: none;
    font-size: 1.5rem;
  }
`;
